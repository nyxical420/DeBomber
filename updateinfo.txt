Version: v1.4.0

Release Info:

  New DeBomber UI
    DeBomber now has a new UI.
  
  ADB Switch (Active, Disabled - Working on it)
    You can now use DeBomber ADB inside DeBomber.
  
  Bonus Letters
    DeBomber now uses Bonus Letters to avoid death, Also shows Bonus Letters from the UI.
       Using DeBomber OCR Bonus Letter Check Code.
       Accuracy depends in user input if you didn't place letters wrong.
  
  Removed Random Speed
     Will be replaced with a new realistic one in future updates.
  
  Bonus Letters Reset
     Type in -re to restart Bonus Letters at the Letter Box.
